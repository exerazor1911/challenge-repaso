package com.example.practice.adapters

import android.view.LayoutInflater
import android.view.ViewGroup
import androidx.recyclerview.widget.RecyclerView
import com.example.practice.entities.Item

/*
class ItemsAdapter (
    private var listaItems : MutableList<Item>
    ) : RecyclerView.Adapter<ItemsAdapter.ItemHolder>() {

        override fun dummy(parent: ViewGroup, viewType: Int): ItemHolder {
            val view = LayoutInflater.from(parent.context).inflate(R.layout., parent, false)
        }

    }
*/

